int temp = usin[i] + usin[i + x];
        // while (temp > 25) {
        //     temp -= 26;
        // }

        // usin[i] += temp;
        // if (usin[i] > 'Z') {
        //     usin[i] = 'A' + (usin[i] - 'Z') - 1;
        // }