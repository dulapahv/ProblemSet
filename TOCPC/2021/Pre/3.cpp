#include <bits/stdc++.h>

using namespace std;

int main () {
    int node, amount;
    cin >> node >> amount;
    int list[amount];
    for (int i = 0; i < amount; i++) {
        cin >> list[i];
    }
    
}